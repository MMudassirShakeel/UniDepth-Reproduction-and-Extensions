from setuptools import find_packages, setup

setup(
    name="unidepth_ext",
    version="1.0.0",
    description="Reusable library code behind the UniDepth mobile-robot reproduction & extensions.",
    packages=find_packages(),
    python_requires=">=3.9",
)
